<%= form_with(model: supporter_notification, url: supporter_notifications_path(current_user, supporter_notification)) do |form| %>
  <% if supporter_notification.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(supporter_notification.errors.count, "error") %> prohibited this supporter_notification from being saved:</h2>

      <ul>
        <% supporter_notification.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :player, style: "display: block" %>
    <%= form.text_field :player %>
  </div>

  <div>
    <%= form.label :message, style: "display: block" %>
    <%= form.text_area :message %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
