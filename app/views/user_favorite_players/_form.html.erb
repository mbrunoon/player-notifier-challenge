<%= form_with(model: user_favorite_player) do |form| %>
  <% if user_favorite_player.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_favorite_player.errors.count, "error") %> prohibited this user_favorite_player from being saved:</h2>

      <ul>
        <% user_favorite_player.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :player_id, style: "display: block" %>
    <%= form.text_field :player_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
