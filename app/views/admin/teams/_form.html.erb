<%= form_with(model: admin_team) do |form| %>
  <% if admin_team.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_team.errors.count, "error") %> prohibited this admin_team from being saved:</h2>

      <ul>
        <% admin_team.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
